# Remove differential expression data

Remove differential expression data

## Usage

``` r
removeDifexp(x)

# S4 method for class 'OmicSignature'
removeDifexp(x)
```

## Arguments

- x:

  An \`OmicSignature\` object.

## Value

An \`OmicSignature\` object with \`difexp\` removed.
