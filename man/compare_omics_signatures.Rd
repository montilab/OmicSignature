% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_omics_signatures.R
\name{compare_omics_signatures}
\alias{compare_omics_signatures}
\title{Compare OmicSignature objects}
\usage{
compare_omics_signatures(
  sig_list1,
  sig_list2 = NULL,
  method = c("overlap", "ks", "gsea"),
  background = NULL,
  score_cutoff = 0,
  adj_p_cutoff = 0.05,
  min_features = 5,
  max_feature = 500,
  label_pairing = NULL,
  label_pairing2 = NULL,
  feature_col = "feature_name",
  score_col = "score",
  adj_p_col = "adj_p",
  group_col = "group_label",
  adjust = FALSE,
  p_adjust_method = "BH",
  alternative = "greater",
  gsea_score = "NES",
  minSize = 1,
  maxSize = Inf,
  nproc = 0,
  ...
)
}
\arguments{
\item{sig_list1}{List of OmicSignature objects or an OmicSignatureCollection.}

\item{sig_list2}{Optional second list of OmicSignature objects or collection.}

\item{method}{Comparison method.}

\item{background}{Optional background feature vector for overlap tests.}

\item{score_cutoff}{Minimum absolute score to include in a signature.}

\item{adj_p_cutoff}{Maximum adjusted p-value to include in a signature.}

\item{min_features}{Minimum number of features retained per label-specific
signature.}

\item{max_feature}{Maximum number of features retained per label-specific
signature.}

\item{label_pairing}{Optional named list giving the two group-label levels
to use for each signature in `sig_list1`.}

\item{label_pairing2}{Optional named list giving the two group-label levels
to use for each signature in `sig_list2`.}

\item{feature_col}{Column containing feature identifiers.}

\item{score_col}{Column containing scores in signature and difexp tables.}

\item{adj_p_col}{Column containing adjusted p-values in difexp tables.}

\item{group_col}{Column containing phenotype group labels.}

\item{adjust}{Logical; adjust p-values within each returned comparison.}

\item{p_adjust_method}{Multiple-testing correction method.}

\item{alternative}{Alternative hypothesis for Fisher and KS tests.}

\item{gsea_score}{Column from fgsea output to return as the score matrix.}

\item{minSize}{Minimum pathway size passed to fgsea.}

\item{maxSize}{Maximum pathway size passed to fgsea.}

\item{nproc}{Number of fgsea workers.}

\item{...}{Additional arguments passed to fgsea.}
}
\value{
A list with one element per label pairing. For `method = "overlap"`
each element contains `jaccard`, `pvalue`, and `counts` matrices. `counts`
entries are formatted as `"ov | n1 | n2"`. For `method = "ks"` and
`method = "gsea"` each element contains `score` and `pvalue` matrices.
}
\description{
Compare signatures within one list, or between two lists, using set overlap,
a two-sample Kolmogorov-Smirnov test, or fgsea.
}
\examples{
data(compare_signatures_example)

overlap_res <- compare_omics_signatures(
  compare_signatures_example[1:2],
  method = "overlap",
  score_cutoff = log2(1.025),
  adj_p_cutoff = 0.01,
  min_features = 10
)
overlap_res$comparisons$level1_vs_level1$jaccard
}
